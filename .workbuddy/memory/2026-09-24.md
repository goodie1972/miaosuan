# 2026-09-24 工作日志

## run.js 一键启动器修复与端到端验证 ✅

### 修复内容（launcher/run.js）
1. **Python 候选顺序**：`.venv\Scripts\python.exe` 提到首位（原顺序先命中系统 `C:\Python314\python.exe`，导致 `ModuleNotFoundError: No module named 'miaosuan'`）。
2. **启动命令**：改为 `python -m miaosuan.cli ui --host 127.0.0.1 --port 8686`（原 `BACKEND_MODULE='dashboard.ui'` 是不存在的模块）。
3. **就绪探测**：`/api/engine/status` → `/api/meta`（该端点真实存在且是只读轻量接口）。
4. **常量**：PORT=8686；文案全部用「妙算」（已全库替换，grep 确认无残留「神机」）。

### 端到端验证结果（node run.js 实跑）
- 端口 8686 在 **2.0s** 内就绪 ✓
- `GET /api/meta` → 200，返回 5 个市场画像（FOREX_XAUUSD / CN_EQUITY_RESEARCH / US_EQUITY_RESEARCH / CRYPTO_BTC / CN_COMMODITY_FUTURES）✓
- `GET /` → 200，index.html 93422 字节、含 title ✓
- `GET /api/acquisition/sources?profile=FOREX_XAUUSD` → 4 个数据源，类型 = **Dukascopy / MT4 / Shenji / TradingView**（MT4 Bridge 正常出现在下拉来源中）✓
- 启动器自动打开浏览器到 http://127.0.0.1:8686 ✓

### 关键教训（可复用）
- **多 Python 环境机器上，launcher 必须优先探测项目 .venv**，否则系统 Python 找不到 editable 安装的包。
- 启动器健康检查要用项目**真实存在**的端点（妙算用 `/api/meta`），照抄别的项目模板前先 grep 端点是否存在。
- PowerShell 重定向输出是 UTF-16，读取前需用 Python `decode('utf-16')`；stdout 经常捕获为空，统一改为「脚本写文件 + Read 文件」模式。

### 文件变更
- M launcher/run.js（修复 Python 优先级、模块名、探测端点）
- 全库品牌名批量替换为「妙算」（README/pyproject/docs/src/tests/templates 等 40+ 文件）

### 环境状态
- 验证时后端（node run.js 后台任务 HESSUc）保持运行，http://127.0.0.1:8686 可访问。


## 仓库清理与提交（第二批）
- 漏网文件补替换：pyproject.toml(1)、factor_kernel_v1.py.j2(3)、index.html(10)——扩展名清单此前未含 toml/j2/html；全库终扫神机残留 = 0
- 新文件 lint/format 修复：test_mt4_bridge SIM117/SIM105/UP041、mt4_bridge UP037/UP041、server RET504、fetchers/__init mypy union-attr（mt4_host 先 strip 再判空）
- mypy 归因：25 错中仅 fetchers/__init__:82 为本次引入（已修），其余 24 为预存债务（cli tune 段/tradingview/okx 等未改动行）
- 测试：全量仅 6 个预存 parity 失败（8000 vs 8203）；提交前门禁（fetchers+mt4+dependency_direction）与冒烟（webui/export/cli/fetchers）全绿
- 提交批次：b74c1f7 chore 清理 / 49d225d refactor(brand) / 1863caf feat(mt4) / 3fa8883 feat(launcher) / 4f1c682 docs

## 三件收尾事项（全部完成）
1. **parity 基线重生成**：Oracle env（torch 2.14.0+cpu）跑 `gen_e2e_xauusd_baseline.py`，8000→8203 根；测试去掉硬编码 8000（改从 e2e_meta/npz 动态断言）、raw 比较加 `equal_nan=True`（volume 前段 NaN）→ 6 个预存失败全转绿，**全量 0 失败**。提交 aa95773。
2. **后端重启验证**：杀掉 8686 旧监听 → `node launcher/run.js` 拉起 → 1s 就绪，`/api/meta` 200，FOREX_XAUUSD sources = Dukascopy/MT4/Shenji/TradingView。
3. **推送双远端成功**：gitee `65bb217→aa95773`、origin(github) `299a9e3→aa95773`，master 均同步。
- 踩坑：该 bash shell 无 sleep/grep/tail；`netstat` 输出为 GBK（Python utf-8 解码会炸）→ 改用 `Get-NetTCPConnection -LocalPort 8686` 杀端口进程树。

## mypy 24 错全清零（提交 a0f3756）
- 9 文件、24 个错误 → `mypy src/miaosuan` EXIT=0 `Success: no issues found in 70 source files`
- **真 bug 修复**（非纯注解）：
  - `cli.py` `result.n_trials` → `len(result.trials)`（TuneResult 无此属性，运行时会 AttributeError）
  - `cli.py` `save_tune_result` 未 import（运行时 NameError）
  - `cli.py` `param_spaces` 变量名冲突：先 `str`（Typer Option）后赋 `list[ParamSpace]`，改名 `param_spaces_json`
  - `server.py` `spec.payload.tokens` 无 isinstance 收窄（ParamPayload 无 tokens）
- **类型注解补全**：fetchers 泛型参数（`list[list]`→`list[list[Any]]` 等）、`_import_tv`/`patched` 返回类型、`_run() -> None`、`json.loads` 返回值显式标注、`_base_url` None 安全
- 附带 `ruff format` 9 文件（预存格式漂移一并修正），全量测试 EXIT=0 无回归
- 双远端推送：gitee `60d7f54→a0f3756`、origin `60d7f54→a0f3756`
