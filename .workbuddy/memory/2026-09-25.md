# 2026-09-25 工作日志

## Spec 管理方案设计

- 完成 miaosuan 项目 Spec 全生命周期管理方案设计，写入 `docs/spec-management.md`
- 方案覆盖四个核心问题：导出决策机制、spec↔.py 映射、谱系追踪与版本管理、完整生命周期支撑
- 核心设计：新增 `SpecRegistry` 作为独立索引层（`artifacts/spec_registry.json`），不侵入 spec 四段结构
- 关键改造点：
  - Provenance 新增 `parent_spec_id` / `lineage` / `derivation`（向后兼容）
  - tune 命令新增 `--derive-spec` 产出派生 spec
  - ExportResult / magic 账本新增 `spec_id` 字段
  - 模板新增 `SPEC_ID` 常量
  - 新增 `lineage` CLI 命令
- 分四阶段实施：Phase1 registry 基础 → Phase2 谱系 → Phase3 回测/tune 记录 → Phase4 导出策略与自动化
