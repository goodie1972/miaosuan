# 2026-09-16 工作日志

## 妙算（MiaoSuan）应用研究 + 运行验证

- 用 software-company 专家团队（架构师 + QA）完成研究与完整测试：919 项用例 915 通过 / 3 跳过 / 1 失败（data/acquisition.py 越权读 os.environ，属代码架构纪律问题，非运行时故障）。
- 完整测试运行方式：`.venv/Scripts/python.exe -m pytest tests`（torch 由 parity/conftest 桩兜底，无需安装）；环境用托管 Python 3.13.12 建 venv，安装 `.[dev,tune]`。
- 启动 Web UI：`.venv/Scripts/python.exe -m miaosuan.cli ui --host 127.0.0.1 --port 8686`（fastapi 0.141.1 / uvicorn 0.52.4 已在 venv 中）。页面只读代理妙算后端（默认 127.0.0.1:1783），缺失时优雅降级。
- 试用种子数据：`data/TESTXAU_H1.parquet`（合成随机数据，门禁必 BLOCKED）。已预跑：mine(quick)→spec.json + 50 代 history；export→`artifacts/20260916_h1_xauusd_miaosuan_v1.py`(magic 661801)；backtest→Sharpe 2.32（随机数据无意义，仅验证管线通）。
- 注意：本机 Bash shim 不支持 `cd`/`tail`/`dirname`，命令须用绝对路径、避免管道到 coreutils。

## 修复：数据获取不可用（两处）

1. **架构纪律违规（已修复，测试转绿）**：`data/acquisition.py` 4 处直接 `os.environ` 读取，违反 `test_os_environ_only_in_config_and_cli`（env 仅允许在 config.py/cli.py）。已按 `shenji_backend_config()` 范式，在 `config.py` 新增 `data_acquisition_config()`/`DataAcquisitionConfig`（env 名 `MIAOSUAN_SHENJI_DB_PATH`/`MIAOSUAN_DATA_API_URL`/`MIAOSUAN_DATA_CACHE_DIR`/`MIAOSUAN_DATA_TIMEOUT`），acquisition.py 改为调用该 accessor。完整测试现 916 通过 / 0 失败 / 3 跳过。

2. **Web UI 数据获取 422 bug（已修复）**：`/api/acquisition/fetch` 与 `/api/tune/start` 的 `FetchRequest`/`TuneRequest` 定义在 `create_app()` 内部（嵌套类），FastAPI 无法识别为 JSON body，把 `req` 当成 query 参数 → 422。已上移到模块级（与 `MineRequest` 等一致）。修复后 fetch 对已有缓存品种返回 200、未知品种返回 400「无可用来源」、tune/start 返回 200。

## 数据源结构调研（用户第 4 个需求：妙算本地 vs 网络）

用户明确：**不要用"方法1"（直接丢 parquet 到 D:\K线数据）**。要两种真实来源作为不同选项：
- **选项 A 妙算本地**：现有 `data/acquisition.py:ShenjiLocalSource` 是 SQLite 桩（假设 `candles(time,open,high,low,close,tick_volume)`，按 symbol+timeframe 过滤），但全项目**无任何 .db/.sqlite 文件、无 CREATE TABLE**，表结构未经验证 → 纯猜测。真正可用的妙算数据接口是 `adapters/shenji/realtime.py:ShenjiReadOnlyClient`，只读白名单 GET `http://127.0.0.1:1783/api/market/candles?symbol=&timeframe=&count=`（`_MAX_CANDLES=2000`），WebUI 03 实时页同源。本环境对该端口探测返回 502（沙箱无法连到用户本机妙算平台），故无法在此抓取真实 candles 响应结构。
- **选项 B 网络/TradingView**：`artifacts/m5_ls.txt` 是用户"AM"项目的目录快照，含真实用过的脚本 `pull_xauusd_tv.py` + `run_pull_xauusd.bat`（TradingView 拉 XAUUSD）、`_test_akshare.py`、`_test_tqsdk_login.py`/`test_tqsdk_limits.py`（天勤）、`download_okx_klines.py`（OKX）、`fetch_all_domestic_futures.py`/`download_pvc_5m.py`（国内期货）；代码里还引用 Binance 涨绿跌红口径。这些都不在 miaosuan 仓库内，需从 AM 项目移植/改写进 `NetworkSource`。
- 待用户澄清：妙算到底走"SQLite/DB 文件直读"还是"HTTP 只读后端 127.0.0.1:1783"（后者单次 ≤2000 根，不适合挖因子长历史）；以及妙算 DB 真实路径/表结构。

## 数据获取"为什么用不了"结论
- 两个真实数据源（妙算本地 SQLite / 网络 API）是桩实现，需环境变量 `MIAOSUAN_SHENJI_DB_PATH` / `MIAOSUAN_DATA_API_URL`，本环境均未配 → 均 `available=false`。
- 因此 `fetch()` 仅对 `D:\K线数据` 或 `data/` 中已存在 `{SYMBOL}_{TIMEFRAME}.parquet` 的品种可用（如 XAUUSD_H1/H4/M15 有真实缓存）；未知品种报「无可用来源」。
- 启用方式：放自己的 parquet 到上述目录，或配置上述 env 指向真实妙算库 / 数据 API。

## 实现：AM 数据源 + 妙算读取程序整合（用户第 5 个需求）
- 真实妙算库定位：`D:\backup\BaoBao\PythonProgram\xauusd\data\market_data.db`（21.7MB，只读 `mode=ro`）。真实表 `ohlcv(timeframe TEXT, timestamp INTEGER[秒], open/high/low/close REAL, volume REAL)`，**无 symbol 列**（单品种库）；另有 `ohlcv_clean`。7 周期 D1/H1/H4/M15/M30/M5/W1；H1≈3017 行。
- 新建 `src/miaosuan/data/fetchers/` 包（BaseFetcher 鸭子类型 + 统一列契约 time/open/high/low/close/volume/tick_volume，升序去重）：
  - `shenji_db.py` ShenjiDBFetcher：只读 URI `file:...?mode=ro`，`symbol` 被有意忽略；增量 `timestamp > since_ts`。
  - 网络源：tradingview（惰性 tvDatafeed + 代理自动探测，env 无关）、akshare（XAUUSD→au0）、tqsdk（KQ.i@XAUUSD）、okx（stdlib urllib 公开 REST）、dukascopy（stdlib urllib freeserv 公开历史 + user/password 预留）。
  - `__init__.py` 暴露 `ShenjiDBFetcher` + `list_network_sources()`（函数内惰性构造、按 `is_available()` 过滤；OKX/Dukascopy 走公开接口恒可用，TradingView/akshare/tqsdk 缺依赖优雅 False）。
- `config.py`：新增 `MIAOSUAN_DATA_SOURCE` / `MIAOSUAN_DUKASCOPY_USER` / `MIAOSUAN_DUKASCOPY_PASS` + `DataAcquisitionConfig` 字段；`_default_shenji_db_path()` 首选 `D:\backup\BaoBao\PythonProgram\xauusd\data\market_data.db`（显式 `MIAOSUAN_SHENJI_DB_PATH` 优先）。
- `data/acquisition.py`：`ShenjiLocalSource` 改委托 `ShenjiDBFetcher`；`NetworkSource` 先试通用 HTTP JSON，再逐 `list_network_sources()` fetcher。
- `tests/test_fetchers.py`：真实库读取（只读 mtime 不变）、网络源实例化、list 过滤、acquisition 注册、config 默认值。
- **依赖方向铁律仍绿**：`src/miaosuan/data` 下 Grep `os.environ|getenv|putenv|from_env` = No matches（env 仅 config.py）。

## 验证结论（lead 层 + QA 层）
- 冻结基准 `D:\K线数据\XAUUSD_H1.parquet` 完好：8000 行 / 6 列 / max_time 1788962400（工程师此前误操作合并到 8109 行后已恢复）。
- `test_dependency_direction_adapters.py` + `test_fetchers.py` = 18 passed；XAUUSD e2e 对拍 = 6 passed（数据路径/特征层/因子层聚合+逐点）。
- ⚠️ 沙箱坑：parity 的 `test_e2e_factor_report_artifact` 用 `tmp_path` → session 收尾清理 `C:\Users\...\Temp\pytest-of-*\garbage-*` 触发 SAFE_DELETE_BULK_REJECTED（用户已拒绝）。**规避法**：跑 parity 用 `-k "not report_artifact"`（其余 5 项不含 tmp_path，干净通过）。临时日志 `_pytest_full.log`/`_pytest_full2.log` 仍在仓库根，删除被沙箱拦截，需用户显式许可再清。

## 重构：「数据文件」模块（用户需求：整合为「数据选择」+「数据获取」双 Tab）
- 触发：用户要求把串行的 `① 数据文件` + `①ᵇ 数据获取` 合并为**单一卡片**的双 Tab（已有数据→选择，无数据→获取），并明确 5 输入框、下拉选项严格来自市场画像、来源单选且须对应画像、获取成功/失败均弹窗。
- **澄清决策**：品种/周期下拉清单 =「扩展模型+合理默认值」（不另建表）；数据模块与挖掘模块的「市场画像」下拉 =「仅共享选项列表」（同来源列表、独立选择值）。
- **后端改动**：
  - `market/profiles.py`：`FrozenMarketProfile` 增 `symbols`/`timeframes`（默认 `field(default_factory=list)`，置于 `limit_pct` 之后，**不进 `MARKET_PROFILE_FIELDS`** 以保持 v1 契约 15 字段不变 → `test_market_profiles` 仍绿；未触碰 `core/` → `test_core_zero_touch` 仍绿）。4 个画像各给合理默认值（如 FOREX→XAUUSD + 7 周期）。
  - `webui/server.py`：`/api/meta` 新增 `profile_details`（name→{symbols,timeframes}）；`FetchRequest` 增 `source`/`note`/`market_profile`；`/api/acquisition/fetch` 透传，返回 path/source/note。
  - `data/acquisition.py`：`fetch()` 增 `source`(单选来源名)/`note`(追加到缓存文件名 `{symbol}_{tf}_{slug}.parquet`)；新增 `_select_sources()`、`_slugify()`（保留中文）。无 note 时文件名不变 → **冻结基准 `XAUUSD_H1.parquet` 不受影响**。
- **前端改动** `webui/static/index.html`：合并为 `① 数据文件` 单卡 + 双 Tab；数据获取 Tab 一行 5 项（市场画像/品种/周期/时间/备注），品种+周期下拉由 `profile_details` 驱动；来源改为单选 `<select>` 且**选定画像前不加载/不可选**；新增弹窗 `showModal/hideModal`（成功/失败均弹窗）；`loadMeta` 同时填充挖掘与数据模块的画像下拉（同列表、独立值）。
- **验证**：`test_market_profiles`+`test_core_zero_touch` = 43 passed；`/api/meta` 返回 profile_details；POST `/api/acquisition/fetch`(source=ShenjiLocalSource,note=验证备注_verify)→200 且生成 `XAUUSD_H1_验证备注_verify.parquet`，基准 8000 行/max_time 1788962400 不变（验证文件已删）。
- ⚠️ **环境坑（重要）**：本机编辑文件间歇性 `EBUSY` 导致 Edit "Successfully edited" 但**写入未落盘**（多处在同批次被静默丢弃，需逐个读回复查）。教训：批量改动后必须**逐处 Read 回读确认**，尤其是 server.py 的 meta 块、profiles 实例种子、index.html 的函数定义块。
- Web UI 已重启：杀掉旧 PID 19316，`.venv/Scripts/python.exe -m miaosuan.cli ui --host 127.0.0.1 --port 8686`（task NBEq51），新代码生效。

## 重构（续）：数据源按「类型」划分 + 与市场画像严格匹配（用户最新需求）
- 需求：数据源按**数据源类型**分类——妙算(Shenji)/TradingView/OKX/Dukascopy/其他，而非按聚合维度；每个市场须用对应数据源，不同市场的数据源各自独立、互不相同、与市场画像严格匹配。
- **后端** `data/acquisition.py`：
  - 新增类型常量 `SOURCE_TYPE_*`（Shenji/TradingView/OKX/Dukascopy/其他）与 `_FETCHER_TYPE_MAP`（fetcher 类名→类型）。
  - `DataSource` 增抽象 `source_type()`；`ShenjiLocalSource`→Shenji，`NetworkSource`→其他。
  - 新增 `TypedNetworkSource(fetcher)`（单 fetcher 包独立源，source_type 来自映射）与 `GenericHttpSource`（通用 HTTP 接口→其他）；`_build_default_sources()` 把每个可用 fetcher 注册为**独立**类型源。
  - `list_sources(allowed_types=None)` / `_select_sources(source_name, allowed_types=None)` / `fetch(..., allowed_types=None)` 均按 source_type 过滤；`fetch` 的 `source` 参数语义由「类名」改为「类型」。
- **`market/profiles.py`**：`FrozenMarketProfile` 增 `data_sources:list[str]`（字段在 `limit_pct` 之后、不进 `MARKET_PROFILE_FIELDS`，v1 契约未破）。4 画像各声明独立来源：FOREX→[Shenji,TradingView,Dukascopy]、CN→[Shenji,其他]、US→[Shenji,TradingView,其他]、CRYPTO→[Shenji,OKX,TradingView]。
- **`webui/server.py`**：`/api/meta` 的 `profile_details` 增 `data_sources`；`/api/acquisition/sources?profile=` 仅返该画像来源；`/api/acquisition/fetch` 据 `market_profile` 算出 `allowed_types` 透传给 acquisition（跨市场选源→400）。
- **`webui/static/index.html`**：`loadAcqSources(profile)` 改为按 `?profile=` 请求、`<option>` 值用 `source_type`（仅显示该画像匹配且可用的源）。
- **验证**：`test_fetchers`+`test_market_profiles`+`test_core_zero_touch` = 48 passed（exit 0）；live e2e：meta 含 data_sources、sources 按画像过滤正确、OKX 在 CN 画像下 fetch 被拒(400)、FOREX 下 Shenji fetch 200 且基准 `XAUUSD_H1.parquet` md5 不变。Web UI 重启（task 2ytiqO）。
- ⚠️ **EBUSY 再度确认**：批量 Edit 仍会静默丢前序编辑（本次丢了 __all__/常量/抽象方法/字段声明/实例种子/meta 块/函数体等）。**对策**：用 Python 脚本 `open().write()` 一次性补回（绕开 Edit 工具锁），逐处校验。临时脚本已清理。

## Dukascopy 模拟账户过期 + 取数自检（用户最新需求）
- 结论先行：Dukascopy 的**历史**蜡烛接口 `https://freeserv.dukascopy.com/2.0/rest/ps/public/candles` 是**公开免登录**的，`DukascopyFetcher` 从不发送 user/password（那两个参数只是未来实盘预留）→ **模拟账户过期完全不影响妙算取历史数据**。真正需要 jForex 账户的是实时流，超出当前实现范围。
- 顺带修掉 `src/miaosuan/data/fetchers/dukascopy.py` 两个缺陷：
  1. `is_available()` 原恒 True → 改为**带缓存的 TCP 探测**（`getaddrinfo(AF_INET)` 只取首个 IPv4 + `create_connection(timeout=probe_timeout)`，结果存 `_avail_cache`；`__init__` 新增 `probe_timeout=3`）。写法对齐 `binance.py`，避免 `create_connection(host,port)` 双栈重试导致 6s 卡顿。
  2. `_fetch_raw()` 的 `except DataError: break` → 改为**抛** `DataError("Dukascopy 拉取中断（窗口 …）")`。原写法会把「网络不通」伪装成「无数据」，误导排查。
  3. `describe()` 不可达时返回 `Dukascopy: 主机不可达（freeserv.dukascopy.com）`。
- 新增用户侧自检脚本 `scripts/check_dukascopy.py`：`.venv\Scripts\python.exe scripts/check_dukascopy.py [SYMBOL] [TF]`，打印主机可达性/来源描述/取数行数/首尾 3 行，退出码 0 成功、2 网络不通、3 取数失败。**沙箱内实测主机不可达（exit 2）**，脚本本身可用。
- 长期有效模拟账户（用户后续要办）：先注册 Dukascopy **社区账户** https://www.dukascopy.com/fxcomm/?language=cn ，社区主页下方有「加长有效期模拟帐户 / Extended validity DEMO」，无限有效期但**至少每 14 天使用一次**；普通 demo 固定 14 天过期。
- **QA 回归**：全量 `EXIT=0` / 933 passed / 3 skipped（79s）。新增 `tests/test_dukascopy_probe.py`（9 例，monkeypatch socket，离线秒级），覆盖可达/不可达双路径、探测结果缓存（连调 3 次仅解析 1 次、False 也缓存）、getaddrinfo 抛错、`describe()` 不可达文案、`_fetch_raw` 遇 DataError 上抛。
- ⚠️ **过期测试断言**：`test_fetchers.py::test_list_network_sources_returns_list` 原硬断言 `'DukascopyFetcher' in kinds`，探测改造后沙箱不可达被正确剔除 → 失败。非源码 Bug（函数契约就是"只返回当前环境可用"），已改为 `("DukascopyFetcher" in kinds) == DukascopyFetcher().is_available()`。**教训：给"恒可用/条件可用"的源写断言时不要写死，要与 `is_available()` 联动。**
- 提交：`e305fd1`（QA 的测试）、`9f69a65`（19 文件 +2287 行，含整个 `src/miaosuan/data/fetchers/` 包——此前一直是 untracked，差点丢失）。清理了会话遗留临时脚本 `_apply*.py`/`_e2e.py`/`qa_probe.py`/`finalcheck.txt`。
- 重启后各画像实返：FOREX_XAUUSD→[Shenji,TradingView]、CN→[AkShare]、US→[TradingView,AkShare]、CRYPTO→[TradingView,OKX]、CN_COMMODITY_FUTURES→[AkShare]。**Dukascopy 与 Binance 在本环境被正确剔除**（主机不可达），换到可达网络会自动出现。
